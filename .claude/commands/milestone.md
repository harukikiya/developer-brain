マイルストーン「$ARGUMENTS」に着手します。いきなり実装せず、次の順で進めてください。

1. **位置づけの確認**: このマイルストーンが `CLAUDE.md` のロードマップと設計不変条件のどこに当たるかを一言で。
2. **サブタスク分解**: レビュー可能な小さい単位に割る。各単位の「完了の定義(Done)」も示す。
3. **設計の分岐点の洗い出し**: 勝手に決めず、選択肢と推奨を提示して確認を取る箇所を明示する（例: M1 なら tree-sitter の言語選定、修飾名の再構成方法など）。
4. **着手**: 合意できた最初の 1 単位だけ実装する。実装中に出た新概念は噛み砕いて説明する。
5. **検査と区切り**: `/preflight` を通し、理解の確認を挟んでから次の単位へ。

不変条件（行番号を保存しない／コアを headless に保つ／References はシンボル間粒度 等）を破りそうになったら、進める前に必ず相談すること。
